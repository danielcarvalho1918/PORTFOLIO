services:
  n8n-gt:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - ./:/home/node/.n8n
    environment:
      GENERIC_TIMEZONE: America/Fortaleza
    restart: unless-stopped
    networks:
      - n8n-network-gt

networks:
  n8n-network-gt:
    driver: bridge
